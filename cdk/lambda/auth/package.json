{
  "name": "owlnest-auth-lambda",
  "version": "1.0.0",
  "description": "Authentication Lambda function for OwlNest",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist *.js *.js.map"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}